{
  "name": "hamburg",
  "version": "0.1.0",
  "description": "AI content pipeline: RSS fetch → Claude Code scoring/generation → publish",
  "type": "module",
  "scripts": {
    "pipeline": "node scripts/pipeline.js",
    "publish": "node scripts/publish.js",
    "setup": "node scripts/setup.js",
    "test": "node --test 'test/**/*.test.js'"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.1",
    "rss-parser": "^3.13.0"
  }
}
